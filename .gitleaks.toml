# gitleaks config — extends the default ruleset.
[extend]
useDefault = true

[allowlist]
description = "Scanner's own files and secret templates (patterns, not real secrets)"
paths = [
  '''\.githooks/pre-commit''',
  '''\.gitleaks\.toml''',
  '''zshrc\.local\.example''',
]
