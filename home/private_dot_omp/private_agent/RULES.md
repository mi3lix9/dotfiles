# Delegated task effort

When choosing the `effort` value for a delegated task, default to `med` and use it whenever reasonably appropriate. Use `lo` only for strictly mechanical or trivial work, and use `hi` only when the task is genuinely complex enough that `med` is inadequate. Do not omit `effort` when creating delegated tasks.
